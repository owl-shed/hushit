# Monolith (Client)

The monolith client is *(as the name suggests)* the client that will contain
all of the Hushit functionality in one executable.

For now this will be the primary client that is developed, but the higher
majority of the code in it will later be extracted and turned into reusable
components to use between the different, specialised clients.
