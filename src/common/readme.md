# Common sub-projects

These projects will contain any reusable code that we want to add.

The [main sub-project](./Common/) should be kept without any dependencies.
Additional sub-projects may be created that will be allowed dependencies, but
this will likely not be needed often.
